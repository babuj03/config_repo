spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: file:///${user.home}/Project_stock_management/Microservice/configRepo
          clone-one-start: true


server:
  port: 8888

